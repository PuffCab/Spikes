const sum = (a, b) => {
  return parseInt(a) + parseInt(b);
};

const differrence = (a, b) => {
  return parseInt(a) - parseInt(b);
};

const product = (a, b) => {
  return parseInt(a) * parseInt(b);
};

export { sum, differrence, product };
